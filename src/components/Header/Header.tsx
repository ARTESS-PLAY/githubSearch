import cl from './Header.module.scss';

const Header: React.FC = () => {
    return (
        <div className={cl.header}>
            <h2>Git search</h2>
        </div>
    );
};

export default Header;
